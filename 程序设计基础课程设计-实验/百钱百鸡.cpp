#include <stdio.h>

int main() {
	int a, b, c, i; //a鸡翁个数,b鸡母个数,c鸡雏个数的三倍,i记录情况个数
	for (a = 0, i = 0; a <= 20; a++) { //假设先买鸡翁a 只
		for (b = 0; b <= (100 - a) / 3; b++) {//那么鸡母只能最多买(100 - a) / 3只
			c = 100 - 5 * a - 3 * b;//自然，鸡雏只能拿剩下的钱买，那么剩下的钱全买鸡雏(100 - 5 * a - 3 * b)*3只
			if (a * 5 + b * 3 + c == 100 && c >= 0 && a + b + 3 * c == 100) {//按照题目要求，百钱百鸡；
				printf("答案%d\n", ++i);
				printf("鸡翁个数为%d\n鸡母个数为%d\n鸡雏个数为%d\n", a, b, c * 3);
				\
			}
		}
	}
	return 0;
}

